import React from "react";
import { Button } from "react-bootstrap";

const Mempool = () => {
    return (
        <div>
            <Button>mine</Button>
        </div>
    );
};

export default Mempool;
